router.post('/', (req,res)=>{
    //llamar al metodo de createCart
})

router.post('/:idCart/product/:idProduct', (req,res)=>{
    /*
    idCart y idProd de los params
    llamar al metodo saveProductToCart(idCart, idProduct)
    */
})