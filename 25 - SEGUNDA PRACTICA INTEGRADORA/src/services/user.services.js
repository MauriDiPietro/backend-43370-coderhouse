import Services from "./class.services.js";

export default class UserService extends Services {
  constructor() {}

  register = async (user) => {
    try {
    } catch (error) {
      console.log(error);
    }
  };

  login = async (user) => {
    try {
    } catch (error) {
      console.log(error);
    }
  };
}
